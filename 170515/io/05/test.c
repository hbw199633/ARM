#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
int main()
{
/*
    pid_t pid = fork();
    if(pid == 0)
    {
	exit(0);
    }
*/

   while(1)
   {
//       printf("nihao  \n");
     sleep(1);
   }

   return 0;
}
